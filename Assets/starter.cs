using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class starter : MonoBehaviour
{
    public GameObject soctame;
    // Start is called before the first frame update
    void Start()
    {
        Instantiate(soctame);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
